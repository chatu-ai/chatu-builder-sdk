# @chatu-ai/builder-sdk-mock

Scripted `BuilderClient` for developing UIs against [`@chatu-ai/builder-sdk`](https://www.npmjs.com/package/@chatu-ai/builder-sdk) without a backend: event replay with configurable latency and disconnect injection. Same event schema as core, so swapping in the real client needs no glue.

MIT
